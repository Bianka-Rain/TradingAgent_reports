# 资料库维护

- 按 README 的 ticker／日期结构归档 TradingAgent 报告，保留原始内容和历史版本；更正另附带日期的勘误。
- README.md 默认英文，顶部链接至 README.zh-CN.md 中文版；两份 README 保持客观简洁，仅说明用途、目录结构、贡献方式及许可。说明变更时同步两种语言并核对链接；新增报告按目录归档，README 不维护研究索引。
- 本仓库仅维护 `main`；外部贡献通过 fork 提交 PR 到本仓库 `main`，由维护者 review 后合并。PR 分支命名为 `<类型>/<简短描述>`，类型用 `reports`（报告）、`docs`（文档与 TERMINAL_LIST）或 `fix`（勘误），描述用小写英文加连字符，涉及日期时写 `YYYY-MM-DD`，如 `reports/tsla-2026-09-07`、`docs/terminal-list`。同步前检查差异，避免覆盖远端新增资料。
- 提交 PR 时使用 `.github/pull_request_template.md`，模板标题、提示和清单保持中英双语；勾选目的，简述改动与原因并列出范围，说明可使用中文或英文。
- 只提交报告和必要说明，不提交密钥、个人账户信息或系统临时文件。
- 保持资料库简单，不引入无关代码、自动化或繁杂维护规则。
- [TERMINAL_LIST.md](TERMINAL_LIST.md) 是跟踪标的控制清单，按其「记录规则」维护：只保留当前跟踪的标的并按板块分组；增删标的或评级变化时更新清单与板块分析，并在变更记录追加一行；移除的标的不保留详情，报告目录不动。
- 用户说“处理一下新 report”“归档新报告”等时，按 [TOUR.md](TOUR.md) 执行 results 人读报告归档，保留源目录。
